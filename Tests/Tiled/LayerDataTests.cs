using FluentAssertions;
using Turnable.TiledMap;
using Turnable.Tiled;

namespace Tests.Tiled
{
    public class LayerDataTests
    {
        [Fact]
        public void Test()
        {
            //Func<int, int, int> readTile = LayerData.GetReaderFunc("eJzbwszAsJVMvA2InwIxueD5qP5R/aP6KdIvxsLAIE4mlgBiABB0Jq0=", Encoding.Base64, Compression.Zlib, 16);

            //int tileAt00 = readTile(0, 0);
            //int tileAt10 = readTile(1, 0);
            //int tileAt01 = readTile(0, 1);

            //tileAt00.Should().Be(948);
            //tileAt10.Should().Be(949);
            //tileAt01.Should().Be(997);
        }
    }
}

